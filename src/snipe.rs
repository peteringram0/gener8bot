use std::thread::sleep;
use std::time::Duration;
use crate::{product::{Product}};

pub fn snipe(product: Product) {

  // let snipe_time = (product.ends_at.time() - Utc::now().time())
  //     .num_seconds() as u64;
  //   println!("Will post bid in {} seconds", snipe_time);
  //   sleep(Duration::from_secs(snipe_time));
  //   // TODO: Get the latest product price
  //   // TODO: Base on last product price .. make bid

  //   let latest = self.get(settings);

  //   self.post_pid(settings, 10);

}